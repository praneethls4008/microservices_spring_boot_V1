server:
  port: 8002

spring:

  application:
    name: VACCINATION_CENTER-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/Microservices_SpringBoot_V1
    username: root
    password: ${MYSQL_DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true


